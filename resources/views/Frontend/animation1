<style>
    .card{
        position: relative;
        width: 1000px;
        height: 350px;
        border-radius: 20px;
        display: flex;
        align-items: center;
        border-radius: 20px;
        transition: 0.5s; 
    }
    .card .circle{
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        overflow: hidden;
    }
    .card .circle::before{
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: #d83123;    
        clip-path: circle(420px at center);
        transition: 0.5s;
    }
    .card:hover .circle:before{
        background: #0d42f0;
        clip-path: circle(600px at center);
    }
    
    .card img{
        position: relative;
        top: 50%;
        left: 50%;
        transform: translate(-50%,-50%);
        height: 300px;
        pointer-events: none;
        z-index: 1;
        transition: 0.5s;
    }
    .card:hover img{
        left: 72%;
        height: 500px;
        z-index: 1;
    }
    .card .content{
        position: absolute;
        width: 50%;
        left: 20%;
        padding: 20px 20px 20px 40;
        transition: 0.5;
        opacity: 0;
        visibility: hidden;
    }
    .card:hover .content{
        left: 0;
        opacity: 1;
        visibility: visible;
    }
</style>

<div class="card">
    <div class="circle"></div>
    <div class="content">
        <h5>{{$livre->titre}}</h5>
        <small>
            <a href="{{url('/livre/categorie/'.$categorie->nom.'/'.$livre->titre)}}">        
                Afficher plus
            </a> 
        </small>                                
    </div>
    <img  src="{{asset('storage/images/livres/'.$livre->photo)}}">       
</div>